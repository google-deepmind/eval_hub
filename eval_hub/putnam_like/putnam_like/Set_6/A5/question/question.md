 A base $b$ palindrome is an integer which is the same when read backwards in base $b$. Show that for any natural number $n$ there is an integer which for at least $n$ values of $b$ has 4 digits in base $b$ and is a base $b$ palindrome.
 
